import {Button} from "react-bootstrap";

import React from "react";

function RejectionSampler({ rejectionValue}) {
    return (
        <div>
            {rejectionValue}
        </div>
    );
}

export default RejectionSampler;
